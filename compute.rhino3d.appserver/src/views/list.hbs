<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link rel="icon" href="data:,">
        <title>SoftlyPlease - Grasshopper Definitions</title>
        <style>
            * {
                font-family: 'Times New Roman', serif;
            }
            body {
                background: white;
                color: black;
                margin: 0;
                padding: 40px;
                max-width: 800px;
                margin: 0 auto;
            }
            .header {
                text-align: center;
                margin-bottom: 40px;
                padding-bottom: 20px;
                border-bottom: 1px solid #ddd;
            }
            .header h1 {
                font-size: 2.5rem;
                margin-bottom: 10px;
                font-weight: normal;
            }
            .header p {
                font-size: 1.2rem;
                opacity: 0.8;
            }
            .definitions {
                display: grid;
                grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
                gap: 20px;
            }
            .definition-card {
                background: #f8f8f8;
                border: 1px solid #ddd;
                border-radius: 10px;
                padding: 20px;
                text-align: center;
                transition: all 0.3s ease;
            }
            .definition-card:hover {
                background: #f0f0f0;
                border-color: #666;
                transform: translateY(-2px);
                box-shadow: 0 5px 10px rgba(0,0,0,0.1);
            }
            .definition-card a {
                color: #333;
                text-decoration: none;
                font-size: 1.1rem;
                font-weight: normal;
                display: block;
            }
            .definition-card a:hover {
                color: #000;
            }
        </style>
    </head>
    <body>
        <div class="header">
            <h1>SoftlyPlease</h1>
            <p>Interactive Grasshopper Definition Solver</p>
        </div>

        <div class="definitions">
            {{#each definitions}}
            <div class="definition-card">
                <a href="/view/{{name}}">{{name}}</a>
            </div>
            {{/each}}
        </div>
    </body>
</html>
