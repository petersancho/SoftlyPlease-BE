<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <style>
        body {
            font-family: "Times New Roman", Times, serif;
            margin: 0;
            padding: 0;
            background-color: #fff;
            color: #000;
        }
        header {
            background-color: #fff;
            color: #000;
            padding: 1em 0;
            text-align: center;
            border-bottom: 1px solid #000;
        }
        nav ul {
            list-style: none;
            padding: 0;
            display: flex;
            justify-content: center;
        }
        nav ul li {
            margin: 0 1em;
        }
        nav ul li a {
            color: #000;
            text-decoration: none;
            font-weight: bold;
        }
        main {
            padding: 2em;
            text-align: center;
        }
        h1 {
            font-size: 2.5em;
            margin-bottom: 0.5em;
        }
        h2 {
            font-style: italic;
            font-size: 1.5em;
            color: #555;
        }
        .example-list {
            list-style: none;
            padding: 0;
            margin-top: 2em;
        }
        .example-list li {
            margin-bottom: 0.5em;
        }
        .example-list li a {
            color: #000;
            text-decoration: none;
            font-weight: normal;
        }
        .example-list li a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                <li><a href="/mcneel-examples">McNeel Examples</a></li>
                <li><a href="/my-examples">My Examples</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h1>{{title}}</h1>
        <ul class="example-list">
            {{#each examples}}
                <li><a href="{{this.url}}">{{this.name}}</a></li>
            {{/each}}
        </ul>
    </main>
</body>
</html>
